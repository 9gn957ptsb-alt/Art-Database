# Matthew Livingston — artist website

## Standing instruction

**Surface the later tasks when they become relevant.** `WIX.md` ends with a
numbered "Still to do" list. Whenever something in a conversation touches one of
those items — the topic comes up, the blocker clears, the artist supplies the
missing piece, or work is being done that would naturally carry it — say so and
offer to do it. Once, briefly, as an offer rather than a prompt to act.

Examples of the trigger: real photography arrives (items 2 and 6 become
possible); dimensions arrive (item 3); the artist mentions the Wix editor or
being at a computer (item 1); paintings are mentioned (item 5); the artist
describes what the token object is (item 7).

Don't recite the list unprompted, and don't re-raise an item the artist has
already declined or deferred in the same conversation.

## Where things are

| | |
| --- | --- |
| `docs/` | **Umbrella Portfolio 2026** — served at `/`, deployed by `.github/workflows/pages.yml`. |
| `docs/v2/` | **Artist Website** — the ongoing site, served at `/v2/`. Its front page is the land. Hand-edited; the generator does not write it. Shares `docs/images/`. |
| `docs/works.json` | The works, as data. Edit this, not `index.html`. |
| `scripts/build_page.py` | Renders `docs/index.html` from `works.json`. |
| `scripts/build_artifact.py` | Flattens the page for publishing as an Artifact. |
| `WIX.md` | The live Wix site: CMS schema, current rows, and the to-do list. |
| `docs/v2/index.html` | **The land**, and the front page of the Artist Website — two heights. The globe is the view of the site: the Earth and the places the collages are in — **no other words on it** (artist's request, 23 Sep 2026: only names of collages at their locations, or anything naming a place; `GLOBE_WORDS = false` in `land.js`), each at its real coordinates (Sydney, Washington ×2, New York, San Francisco, Boston, Blacksburg) plus the Folger Shakespeare Library, which is a real building and where the plays are, and **the Archive** in Austin, where the moving images are (see `docs/v2/systems.js`). Drag sideways to turn the world, up and down to roll it north or south. Pressing a city flies you down into it, with that city's collage laid out beside it; the library is the theatre (see `scripts/build_theatre.py`), and the Archive is a wall of monitors. **The globe is alive** (up there only; it fades out on the way down): lit by the real sun at the moment it is looked at, night lights on the dark side, cloud on the real wind bands with lightning in the storms, and **the company** migrating between the places (artist's request, 23 Sep 2026: the characters "built more towards me"; he named Batman, Cormac McCarthy, Nietzsche and Dostoevsky, and will add more). `S.figure` in `systems.js`, `COMPANY`/`CAST` in `land.js`: a sorrel horse (Raskolnikov's dream, Nietzsche in Turin, McCarthy's horses; trots, and when touched it stops and will not be hurried); Zarathustra's three metamorphoses as one traveller that changes on every arrival, camel (pacing, loaded) → lion → child bowling a hoop → camel (the eternal return); Zarathustra's eagle with the serpent round its neck; a man and a boy with a cart, always going south, the boy carrying the fire (it glows at night); and a bat, out only where it is night on the real Earth. **Sparingly** (artist, 23 Sep 2026: "go a little bit easier … it still gimmicky"): one at a time, one journey each (the metamorphosis makes its three and goes; the bat circles the dark for 21–34 s), then 34–89 s of empty world before the next (`CAST_GAP`). The artist's steer: what brings people back is enjoying the process of finding things — new ways of connecting information — not decoration. Nothing on the page names any of them, and no trademarked character is drawn. `COMPANY = false` brings back the invented herd (`S.creature`), which the Archive's Migrations monitor still plays. **The procession over the rim is off** (artist's request, 23 Sep 2026: "too much") — `PROCESSION = false` in `land.js` keeps its code; the Archive's Infinity monitor still plays it. **The creature is out of the cities** (artist's request, 23 Sep 2026: "I don't quite know where I want it right now") — `CREATURE = false` in `land.js` keeps all its code for when it goes somewhere. **The Artist Website is one page with one link**: pressing a place flies to it (the all-collages table survives only for old `works.html` links; the Collages button was removed at the artist's request — nothing floats in the sky). Nothing links to another page, and the address never changes as you move around it — the artist wants one link to everything, so never hand out deep links. **At its site a collage is read, not dealt** ("the reading", `readHere`, `READING = true`; artist, 23 Sep 2026: "very, very specific, transformational, and elegant … things don't have to move very fast … timing is everything, and having the right word pop up at the right time"). A fixed composition — the photograph large on the collage's own side, a column beside it (stacked and scrolling below the banner on a phone) — and a slow clock, set from research on how long people really look at art (Smith & Smith 2001, median 17 s at the Met; Carbon 2017; slow looking): the photograph alone, then title 4 s, place 9 s, medium and size 16 s, price 24 s; the first of its `terms` at **30 s**, then one at a time with gaps that lengthen, 22 → 30 → 40 s (capped), rarest first and the most shared last (`OPEN_AT`, `FIRST_WORD_AT`, `WORD_GAP*`). The clock only advances while the pointer is still (`afterStill`, `STILL_MS`), so words arrive during looking, not fidgeting; pressing or turning never waits on it. **Turning re-deals the composition** (artist, 23 Sep 2026: he liked how turning "changed its position on the screen and re-organized all the information"): the arrangement is dealt from the work and its quarter-turn (the existing `seedFrom`), so each turn swings the photograph to a new side, size and standing and the column eases across with it, while what is already revealed stays; each word showing the other collages that share it as small doors (or "only here"); then all the words in a line, then notes, vote and "Put it away". Pressing a word holds it: the photograph steps back and the doors grow with their names. Pressing a door flies there, and the word you came by is the first thing said on arrival (`cameBy`) — a visit is a walk from collage to collage by what they are made of. The city behind is washed while it is read; the banner stays sharp above. This replaced the random dealing at the site (still used by the all-collages table); `READING = false` brings it back. Every event on the page is answered in **pixel light**, after the animation Claude Code shows while it works (artist's request, 23 Sep 2026): a screen-space grid of 13px tiles on `#tiles` (`pulse`) lights in square-ish rings with ragged edges, three echoes at φ ratios, four stepped brightnesses, held frames at 24 fps; `sparkle` throws square sparks and pixel notes off the creature's head; each dealt photograph is uncovered tile by tile (`pixelIn`) and each caption wipes in in steps; the creature's poses and blink and the telescope's light run on `steps()`. Its colour is the cities' own and a lavender (`LIGHT`). On top of that, **the repertoire** (artist's request, 23 Sep 2026: as many animations as can be found): every event is answered by one animation dealt at random from several, the way the collages are dealt — a photograph arrives by `ARRIVALS` (tiles, mosaic, cells, bands, blocks, glitch, dither, interlace, lift), is pointed at by `POINTED` (glitch, tilt, echo, mosaic), a caption arrives by `ENTRANCES` (wipe, decode, type, block, blur, rise), a globe word and the city banner `scramble`, the pointer leaves lit tiles (`tread`) and, in the far views, the telescope's photographs (`dropHubble`, `#trail`), going down, coming up, opening and closing sweep cells across the window (`sweepCells`, `#sweep`), and the round buttons lean toward the pointer (`MAGNETS`). Most are after Codrops demos, whose source is on GitHub (github.com/codrops) and can be cloned and filmed from a session even when the showcase sites are blocked. A text piece is never dealt narrower than its longest word. `land.css`, `land.js`, `land.json`. |
| `docs/v2/systems.js` | **The systems** — generative animations after the works named in the research the artist shared on 23 Sep 2026 (a ChatGPT share, "Research Digital Animation"; the ask: "incorporate absolutely all of it"). Each is a system that makes moving images out of the site's own material; `land.js` decides where they appear, and anything after someone says so. Where each item lives: **Ryoji Ikeda, *datamatics*** → `datamatics`, dealt among the transitions (`dataSweep`, `passage`); **Refik Anadol, *Unsupervised*** → `Hallucination`, the collages' measured colours as a fluid: half the time it is what a word's collages float over (`dream`), and the `hallucination` arrival and `POINTED.melt`; **GMUNK, *Synapse Code*** → `synapse` (arrival and pointed); **Quayola** → `strataStages` (the `strata` arrival, and the theatre's carved rise); **Universal Everything, *Primordial*** → `cellStages` (the `primordial` arrival) and `Reaction`, Gray-Scott reaction-diffusion at its mitosis settings (the loader, `#primordial`); ***Migrations*** → `creature`, six real gaits (an elephant's walk, an ostrich's run, a kangaroo's hop, a caterpillar's crawl, a bird's swoop, an insect's tripod) on bodies made up each time — once the herd on the globe, now the Archive's Migrations monitor; the globe's travellers are the company (`S.figure`, `stepHerd`), on the same gaits' principle; ***Infinity*** → `person`, the procession over the rim (`stepProcession`, off on the globe, `PROCESSION`; still on the Archive's monitor); **Radiohead and UE, *PolyFauna*** → the globe's weather (`drawWeather`) and `polyFauna`, a small world that answers the pointer; ***Transfiguration*** and ***Walking City*** → the theatre (see `scripts/build_theatre.py`); **Oskar Fischinger, *Motion Painting No. 1*** → `motionPainting`, the theatre's entr'acte; **David OReilly, *The External World*** → `externalWorld`, deliberately crude; **the Austin Museum of Digital Art's archive** → **the Archive**, a landmark in Austin (`LANDMARKS`, `CHANNELS` in `land.js`): twelve monitors, one per system, each "after …", brought up close on a press; its foot credits AMODA's archive in plain words — no link (artist's request, 23 Sep 2026: the Vimeo link was dropped so nothing leads off the site but NASA's — and, since 24 Sep 2026, each building's article). The real sun (`sunNow`) and the night lights (`drawLights`) belong to the living globe. **Sound** (`Systems.sound`) is off until the banner's Sound switch is pressed, every visit: clicks and high sines under datamatics, a figure in G on the motion painting's beat. Under reduced motion only the night and the cloud show, held still. |
| `docs/v2/works.html` | Where the works page used to be. Forwards into the one page, opening the collages, so links to it that were shared before still work; the address is then put back to the plain link. |
| `docs/v2/land.html` | Where the land used to be. Forwards to `./`, so links to it that were shared before still work. |
| `scripts/build_land.py` | Writes `docs/v2/land.json` from the private Artsy dump. |
| `scripts/fetch_artsy_saves.py` | Fetches that dump into `data/`, which is gitignored and stays that way — the repo is public. |
| `scripts/fetch_architecture_saves.py` | Fetches the artist's **Architectural Authority** bookmarks (the "A. Authority" app, `thearchitecturalauthority.com`) into `data/architecture_saves_raw.json`, private and gitignored. Not a connector, so no proxy-injected credential: it signs itself in with the site's **AWS Cognito refresh token**, env var `AA_REFRESH_TOKEN` (set in the environment's settings, never pasted in chat; lasts weeks — the artist copies a new one only when it is refused), trading it for fresh tokens every run so new bookmarks arrive untouched like the Artsy saves (artist's request, 24 Sep 2026); `AA_TOKEN` is the old one-hour fallback. It sends the token as the app's front end does (`GET /api/account-data/Social/me/bookmarks`, `Authorization: Bearer …`). `--probe` prints one record's shape. The bookmarks are magazine articles, each about a building. Added at the artist's request (23 Sep 2026) to connect the bookmarks page "just like the art he saved artworks page." |
| `scripts/build_architecture.py` | Distils that dump into `docs/v2/architecture.json` — the small public file, committed. Each building's place comes from its article's **public** page (`/article/<slug>`, `articleLocation`: city/state/country, no token), geocoded once via `nominatim.openstreetmap.org` (must be allowed under Network access; cached in `data/geocode_cache.json`). Writes only what a globe needs — title, short `name`, `where`, point, `precision`, the link back — never that it was a saved list. The bookmarks give only an `article_id`; the script maps it to a slug by reading the `articleId` on each article page in the site's sitemap (cached in `data/article_index.json`). **Private homes are never pinned closer than their town** (artist's choice, 24 Sep 2026 — the site and repo are public); public buildings get their own spot, looked up by hand and kept in `SPOTS` with its source. `precision` is exact / street / district / town / region; a building with only a country gets no point. **On the globe** (artist's request, 24 Sep 2026): each building is a small square (`data-kind="building"`, from `architecture.json` in `found()`); pressing one flies down as to a city and opens on **the building itself, modelled from the article's photographs** (`docs/v2/models/<slug>.json`, format in `docs/v2/models/MODELS.md`: boxes, gables, sheds, cylinders, domes, blobs, cuts, pools, trees, each of a material; voxelised and drawn as DIRT dots by `docs/v2/models.js`, which `scripts/preview_model.js` also uses to render a model from four sides for checking against the photos — the photos are reference only and never enter the repo). A tap swaps it for **a clod of its ground in DIRT** (`shapeClod`), and back. **Both are shown in true isometric** (artist, 24 Sep 2026: so measurements read true anywhere in the view): parallel projection from arctan(1/√2) above, resting on the four 45° diagonals, swinging a quarter turn every 22.5 s; a drag turns it freely and it settles on the nearest diagonal. **SketchUp is part of the pipeline** (the artist's paid SketchUp, through the Trimble SketchUp connector): forms the part types can't shape are built there at real dimensions and read back as `mesh` parts — `scripts/sketchup/` holds each building's build script, `read_back.py`, `to_model.py` and the steps (README). Disney Hall (the Gehry tribute) is the first. Each `.skp` is fetched from `save_model`'s link (redirects followed; `api.sketchup.com` is allowed) into `sketchup/` at the repo root, outside the published site, for the artist to open. A `.skp` is re-committed only when a building's form really changes (git keeps every binary version); not copied to the Obsidian vault (artist, 24 Sep 2026: too costly in usage). The artist's standing ask (24 Sep 2026): render every building as well as it possibly can be, and keep refining them — continuously. The Google key and its `GOOGLE_MAPS_KEY` secret stay in place at his request, unused. with a link to its article — **the one other way off the site besides NASA's, at the artist's request**. It replaced a Google photorealistic 3D view the same day: the artist wants nothing that can ever be billed, so **no paid map service is used** — the ground is read once at build time from free data by `scripts/build_grounds.py` (for the buildings and the museums; many new places are read in batches of 24 from one pass over Overture; Overture Maps' public S3 bucket via pyarrow, largely OpenStreetMap; Mapzen terrarium tiles on AWS for heights) into `docs/v2/grounds/<slug>.json` (80×80 cells: land/water/road/building, ground height, storeys). Every cell is one dot of DIRT from the spot on the globe the place is at (water from the sea's DIRT, roads gone to dust, buildings rising in stone-pale storeys), the clod's edges go down into the soil, it rises a row at a time. The data's credit line was taken off the view at the artist's request (24 Sep 2026); it stays in the page source. The square is as exact as the point (`SIDE` by `precision`), so a private home's clod is its town. Run it one place at a time — the planet-wide files exhaust memory in parallel. **Every kind of building: `scripts/update_buildings.py`** runs the whole intake for each kind in its `KINDS` (the Architectural Authority bookmarks, the museums from the Artsy saves; fetch → place → ground → photos) and lists, kind by kind, the buildings still to model. **Continuous refinement covers every kind** (artist's standing ask, 24 Sep 2026: "as we add types of buildings, I will want you to continuously refine all the buildings"): the daily routine (09:00 UTC, "Artist Website — all buildings: intake and refinement", in the session "Architecture (daily)") follows `docs/v2/models/REFINE.md` — new bookmarks modelled the same day, three first models a day from the other kinds' backlogs, and the three buildings refined longest ago, of any kind, refined. A new kind is a row in REFINE.md's table, an entry in `KINDS`, a filter in `LAYERS`, and `"kind"` in `ledger.json`. |
| `scripts/build_museums.py` | Writes `docs/v2/museums.json` — **the museums that hold the artist's saved Artsy works** (artist's request, 24 Sep 2026: "do the same thing … for every major art museum that has one of the artworks that I have saved"), each with the works it holds (title, artist, date, medium, a picture off Artsy's CDN; up to 34 each, most recently saved first). A work's museum is the Artsy partner that listed it when that partner is a museum ("Institution" in the "museums" category, minus the auction house, touring body, agency and association in `NOT_MUSEUMS`), or the museum named in its `collecting_institution` (exhibition credits and private collections are not holdings). Placed at Artsy's partner location, checked against OpenStreetMap (Artsy had SFMOMA on City Hall and LACMA 10 km off); museums only named in a credit are geocoded on Nominatim; hand fixes in `QUERIES`, `SPOTS`, `ARTSY_RIGHT`, `ALIASES`. Caches in `data/`. 123 museums, 769 works at first build. **On the globe** they are the **Museums** layer: small diamonds (`data-kind="museum"`) with their names (artist, 24 Sep 2026: "make sure the names of all the gallery museums actually come up"); pressing one flies down to the museum modelled in DIRT (`models/museum-<partner>.json`, same format and pipeline as the buildings, in the ledger with `"kind": "museum"`) on its ground (`grounds/`), with its works in a column beside it (a strip below on a phone; `showHeld` in `land.js`) — pressing a work brings it up large. No link off the site. Only the three with most works were modelled at first (National Gallery of Art West Building, Guggenheim, SFMOMA); the daily routine models three more a run (`REFINE.md`, step 2b). |
| **The filter** (`#filter`, `LAYERS` in `land.js`) | The globe carries the landmarks always, and **one layer at a time** of everything else — **Collages** (the artist's own works, at their places), **Museums** (the museums that hold the works he saved) or **Architecture** (the Architectural Authority buildings) — so it is never overwhelmed and his collages are never confused with the museums' artworks (artist's requests, 24 Sep 2026; "we will need to expand this filter system as we add new types of buildings"). A new kind of place is one more entry in `LAYERS` and a `layer` on its marks in `found()`. A pill at the foot of the globe, hidden below it; the choice is kept per viewer in `localStorage`; Collages first. Every mark carries its name; names that would print over one another wait until the world turns. |
| `scripts/build_earth.py` | Writes `docs/v2/earth.json` — the land mask the weave is shaped by. Natural Earth **50m**, public domain, 1440×720 (¼°); the download goes to `data/`. Column 0 is **180°W** — land.js reads longitude from there (it once read from Greenwich, which put every continent half a world off). City marks sit exactly on their coordinates; coincident places stack their names, never move their dots. |
| `scripts/build_dirt.py` | Writes `docs/v2/dirt-land.png` and `docs/v2/dirt-sea.png` — one pixel a cell, colour and dot size only. The land is **DIRT** (the collection soil from the DIRT session, branch `claude/digital-dirt-layers-paiial`); the sea is the same soil woven again from the saved paintings' blues. The globe's threads show the soil through them, and the globe's body and each family of threads fade between 0.382 and 0.618 on golden-ratio periods. Over that, every word on the globe is the centre of a patch (sized and paced by the word's mass) that fades between 0.382 and 1 on periods of φ³–φ⁶ s — the word placement dictates the transparency, and a fade band runs pole to pole every φ⁵ s; the weave has φ× the threads to make up for it. Grit (baked into the globe's body) and blur (a small copy on its own canvas, `#world-soft`, stretched back up) grow from 1/φ² at orbit to full / 1/φ in a city; opening a word blurs the stage behind its collages, and the world stops painting while collages are open. The sky is the stage's CSS background. A clear coat (hot spot, softbox strips, rim) is drawn on `#world-gloss` only when the globe moves on screen, and hidden in cities. Resolution starts at the screen's full density (up to 3×) and `sharpen()` steps it down half a step at a time if it can't hold ~45 fps. The globe's distance is dealt log-uniformly from φ⁻⁴ (a small world far off) to √φ on every visit, and it swings to a new distance every φ⁵–φ⁷ s when nobody has touched anything for φ⁴ s (`swing`, with a CSS perspective lean on the stage); pressing a world small enough to be seen whole fires it in round the point pressed (a "rocket": wind-back, burst with speed lines/flash/ring on `#burst`, motion blur, perspective kick, shake, spring); tapping the sky bounces to a new framing; pinch (stage is `touch-action: none`), wheel and dragging the sky move the view by hand; `keepHold()` never lets the world be stranded off-screen. Names hide when the world is far; words scale with its distance. No coastline is drawn — the land is its own edge. Coming up from a city restores the exact view. No titles or artists are written — the saved list stays private. |
| `scripts/build_theatre.py` | Writes `docs/v2/theatre/` — **the theatre at the Folger**: thirteen scenes from Shakespeare (the Chorus of Henry V on the Globe's own stage, the Ghost, the witches, the balcony, the Tempest's storm, Yorick, the quarrel, the Dream's wood, the sleepwalking, the Forum, Lear's storm, Falstaff, Ophelia) and the library itself, drawn as isometric pixel art on clods of **DIRT** after the reference sheet the artist sent (not blocks: the voxel figures read as Lego and were dropped, 23 Sep 2026). `scripts/theatre/` is a small signed-distance-field renderer at the picture's own resolution: hand-picked ramps, light from the upper left, narrow-band Bayer dither, selective outlines, faces put in pixel by pixel, the earth read cell by cell off `dirt-land.png` (the sea's off `dirt-sea.png`). Each scene is 4 loop frames plus 4 per speaker, packed as the first frame and the rectangles that change; `theatre.json` holds the lines (Shakespeare's), who says them and where their heads are. In the Folger view (`land.js`, "the theatre") a scene rises out of the soil a row at a time, its people say their lines, it sinks back and the next is dealt; pressing it takes the next line, the bill steps back or on. The Chorus walks toward you and never arrives, made of fire, water, bubbles, smoke, flowers, wood, marble and DIRT in turn (after Universal Everything's *Transfiguration*: a spec's `looks`, each a loop of its own, cycled every 2 s by the page); the library has six legs folded under it and now and then stands and walks (after *Walking City*: the spec's `moves`, `stand` and `walk`, `stepLibrary`; pressing it gets it up). A scene may come up carved out of marble (after Quayola, `carveFrame`), and between scenes, now and then, an entr'acte after Oskar Fischinger's *Motion Painting No. 1* is painted on the stage. A patch is the whole of its rectangle: the page clears it before laying it down. Scenes are edited in `scripts/theatre/scenes.py` and rebuilt with `python3 scripts/build_theatre.py [--only key]` (about six minutes on four cores). |
| `scripts/build_tones.py` | Writes `docs/v2/tones.json` — the colours measured off each collage's own photograph, which is what the continents wear. |

`docs/` and the Wix site are separate. Changes to one do not reach the other.

The GitHub Pages site is the real one: <https://9gn957ptsb-alt.github.io/Art-Database/>.

**The repository was renamed** from `Color-Middling` to `Art-Database`. Git
pushes to the old name still work, because GitHub redirects them — which is
exactly why it went unnoticed — but Pages serves from the current name, so
any link built from the old one is a 404. The working copy may still sit in a
directory called `Color-Middling`; that means nothing. Build links from
`Art-Database`, or better, read the URL out of the deploy: the pages workflow
logs `Evaluated environment url:` on every run.
The two versions have names:

- **Umbrella Portfolio 2026** (`docs/`, served at `/`) — the link submitted to an
  open call. **Frozen**; change it only when asked.
- **Artist Website** (`docs/v2/`, served at `/v2/`) — the site that carries on
  from here, and where work happens now.

The two were forked on 16 Sep 2026. `scripts/build_page.py` writes only `/`; do
not point it at v2 again without saying so, as it would overwrite the redesign.
A change to `works.json` no longer reaches v2 — make it in both places if it
belongs in both.

The live site's design is the one the Wix assistant built, not `docs/`. `docs/`
is a reference for the works and their captions; it is not a target the Wix site
is matched to. See "Matching the reference design — SUPERSEDED" in `WIX.md`.

## Working preferences

- **Whenever the words "Artist Website" appear in a reply, write them as a
  link to the site: [Artist Website](https://9gn957ptsb-alt.github.io/Art-Database/v2/).**
  Every time, the way the DIRT session always links the word DIRT to its
  artifact (artist's request, 23 Sep 2026). That link is the one link to
  everything — never a deep link into the page.
- Don't paste a bare URL on every update — say it's done; the linked words
  carry the link. Keep publishing the updated artifact each time.
- The work photographs are placeholders until shot properly. Don't crop or
  retouch them. They are wanted on the live site as they are — but images cannot
  be uploaded to Wix from a session (see `WIX.md`), so the artist uploads them by
  hand and a session then matches them to rows.
- Writings about a collage — the artist's or anyone's — go in `writings` on that
  work in `docs/works.json`, as `[{"text": "...", "by": "..."}]`. The Artist
  Website deals them with the collage. There are none yet; never write them.
- Don't invent artwork metadata. Titles and years come from the artist;
  dimensions are blank until given.
- `data/` never gets committed. It is the artist's whole Artsy saved-works list
  and this repository is public. Only `docs/v2/land.json` — the distilled
  fraction `scripts/build_land.py` writes — belongs in git.
- The words come from `terms` in `docs/works.json` and link to **his**
  works — now at each collage's site, not on the globe. No Artsy work is ever
  what a word stands on.
- The token objects the creature turns up — three colours to throw — are the
  **Hubble Space Telescope's photographs** (artist's request, 23 Sep 2026),
  read live in the visitor's browser from NASA's image library
  (`images-api.nasa.gov`), colours measured off each thumbnail, each linking to
  its `images.nasa.gov/details/<id>` page. The Artsy saves in `land.json` are
  only the fallback while those load or if NASA can't be reached. NASA's hosts
  are blocked from the session sandbox, so this can only be tested there
  against a stand-in (`page.route`). A Hubble telescope orbits the globe in the
  far views (`placeHubble`); pressing it opens one of its photographs.
